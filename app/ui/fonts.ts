import { Fira_Sans, Lato } from 'next/font/google';
 
export const fira_sans = Fira_Sans({ weight: ['600'], subsets: ["latin"]});
export const lato = Lato({ weight: ['400', '700'], subsets: ['latin']});
